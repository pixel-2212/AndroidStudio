<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.inacap.pixel.prueba2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Opciones" />
        <activity android:name=".Cliente.Clientes" />
        <activity android:name=".Pedido.Pedidos" />
        <activity android:name=".Cliente.AgregarCliente" />
        <activity android:name=".Cliente.VerClientes" />
        <activity android:name=".Cliente.EliminarCliente" />
        <activity android:name=".Pedido.AgregarPedido"></activity>
    </application>

</manifest>